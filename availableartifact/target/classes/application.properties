spring.application.name=availableartifact

# server.port=8082 --


## default connection pool
# spring.datasource.hikari.connectionTimeout=20000 --
# spring.datasource.hikari.maximumPoolSize=5 --

## PostgreSQL
# spring.datasource.url=jdbc:postgresql://localhost:5435/postgres --
# spring.datasource.username=postgres --
# spring.datasource.password=postgres --

#drop n create table again, good for testing, comment this in production
#spring.jpa.hibernate.ddl-auto=create


# -----------
## default port is 8080
server.port=8082

## HikariCP (connection pool)
spring.datasource.hikari.connectionTimeout=20000
spring.datasource.hikari.maximumPoolSize=5

## PostgreSQL
spring.datasource.url=jdbc:postgresql://localhost:5435/crsdb
spring.datasource.username=docker
spring.datasource.password=docker

## Schema
spring.jpa.properties.hibernate.default_schema=slotschema

## Hibernate DDL mode
spring.jpa.hibernate.ddl-auto=validate
